# Git LFS is currently disabled because the ML models are small (~10 MB total).
# We are using normal Git for simplicity.
# If future model files become large (50–100 MB+), uncomment the lines below and run:
#   git lfs install
#   git lfs track "*.pt"
#   git lfs track "*.pkl"
#
# artifacts/** filter=lfs diff=lfs merge=lfs -text
# notebooks/** filter=lfs diff=lfs merge=lfs -text
