# Production API base URL (no trailing slash)
# Local dev: leave empty to use Vite proxy, or set http://localhost:8000
VITE_API_BASE=http://localhost:8000
# Optional: Google Maps key for location features
VITE_GOOGLE_MAP_KEY=your-key
